#{nombre.upper()= vuelve mayuscula texto
# ver como mejorar sintaxis al preguntar indico mi famili y ni puede resppnder mi familia debe ser tu familia
#print(f"Precio: {precio:.2f}")  # 2 decimales
prueba = "alice, andres, joel"
def ignore_partition():
    nombre, _, apellido = prueba.partition(",")  
    print(prueba.partition(","))

ignore_partition()
#lower() → convierte el texto a minúsculas. Ejemplo: "IPHONE".lower() → "iphone".
#strip() → elimina espacios en blanco al inicio y al final. Ejemplo: " Iphone ".strip() → "Iphone".

#3. ¿Qué otras funciones hay parecidas a pop()?
#Aquí van las más usadas con listas:
#pop(i) → elimina y devuelve el elemento en la posición i.
#remove(valor) → elimina el primer elemento que coincida con ese valor.
#clear() → borra toda la lista.
#append(valor) → agrega un elemento al final.
#insert(i, valor) → inserta un valor en una posición específica.
#extend(lista) → agrega varios elementos de golpe.